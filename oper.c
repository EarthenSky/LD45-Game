#ifndef RAYLIB
    #define RAYLIB
    #include "raylib.h"
#endif

// Id is which type of background you want
Texture2D ConstructBackground(int id, Texture2D *tile) {
    // this should return a loaded texture.
    //Texture2D out = GenImageColor(tile->width, tile->height, WHITE);
    //ImageResize(Image *image, int newWidth, int newHeight);                                        

    //return out;
}

//def fact(n):
//    if n==1: return 1
//    else: return n*fact(n-1)